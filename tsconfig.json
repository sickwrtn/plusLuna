{
    "compilerOptions": {
      "target": "es5", // content_scripts는 ES5를 지원해야 함
      "module": "commonjs", // CommonJS 모듈 시스템 사용
      "outDir": "./dist", // 컴파일된 파일이 저장될 경로
      "rootDir": "./source", // TypeScript 파일이 있는 경로
    },
  }